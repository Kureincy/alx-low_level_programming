#include "main.h"

/**
 * _abs - check main
 * @r: An integer input
 * Description: This function returns the absolute value of a number
 * Return: Absolute value of a number r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
